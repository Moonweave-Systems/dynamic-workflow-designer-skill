# V0.5 Decision

Date: 2026-06-14

Decision: keep

## Evidence

Command:

```bash
python scripts/evaluate_plan.py --manifest fixtures/v0.5/manifest.json --out out/v0.5
```

Generated summary:

- 12 fixtures evaluated.
- Candidate keep/kill average: 1.883.
- `workflow-router-skill` baseline average: 1.317.
- `claude-agent-workflow-designer` baseline average: 0.8.
- Summary path: `out/v0.5/summary.json`.

The candidate beats both baselines by more than 20 percent across activation
discipline, handoff clarity, verification strength, safety gating, and
downstream consumer success.

## Fixture Outcome

- Four positive fixtures activate.
- Four negative fixtures downgrade.
- Three borderline fixtures downgrade to `workflow-router`.
- The meta/runtime fixture activates to a backlog-oriented execution path.
- Every fixture has a schema-valid artifact or valid downgrade artifact.

## Baselines

The two confirmed local baselines are recorded per fixture under
`samples/v0.5/baselines/`:

- `workflow-router-skill`: source-hashed normalization failure because the
  source is a routing policy, not a `workflow.plan.json` producer.
- `claude-agent-workflow-designer`: source-hashed normalization failure because
  the local source provides prose workflow guidance and scaffolding helpers, not
  per-fixture schema-valid JSON artifacts.

Baseline records do not contain hand-authored numeric scores. The evaluator
derives scores from structured observations and verifies the current source hash
before using each record.

## Consumer Result

Deterministic blinded-consumer records are stored per fixture under
`samples/v0.5/consumer/` and regenerated under `out/v0.5/consumer/`.

Result: pass. Each consumer report uses only the allowed blinded inputs,
identifies the exact first-slice inputs, expected output, completion check,
forbidden actions, risk gates, and safe defaults, and does not receive this spec
or expected-answer notes. Downgrade fixtures also agree with the downgrade
target.

## Raw Artifacts

Tracked raw outputs are stored under `samples/v0.5/raw/`. The evaluator rejects
raw output that merely duplicates the parsed plan; each raw record wraps the
prompt, rendered blueprint, and parsed `workflow_plan`. The evaluator rejects
prompt mismatch or blueprint drift before regenerating
`out/v0.5/<fixture>/raw-output.json`.

## Boundary

V0.5 does not claim runtime execution or live model generation from `SKILL.md`.
It proves that the tracked fixture corpus can be serialized as
`workflow.plan.json`, rendered as a blueprint, and evaluated deterministically
before a future plugin or runtime slice.
